# ============================================================
# Nginx - Pasa La Peli (Frontend Angular 18)
# - Sirve la SPA con fallback de rutas
# - Proxy /api/ -> EC2-APPS (host inyectado en build via ARG EC2_APPS_HOST)
# - HTTPS obligatorio (certificados montados en /etc/nginx/certs)
# ============================================================

# Todo el trafico HTTP se redirige a HTTPS
server {
    listen 80;
    server_name _;

    location /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
    }

    return 301 https://$host$request_uri;
}

# Servidor HTTPS principal
server {
    listen 443 ssl;
    server_name _;

    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy para llamadas a la API Gateway / BFF
    # El host se inyecta en build via ARG EC2_APPS_HOST (Dockerfile).
    #   - Topologia 3 EC2:  EC2_APPS_HOST = <IP publica de EC2-APPS>
    #   - Single EC2 / local: EC2_APPS_HOST = bff-service (default)
    location /api/ {
        proxy_pass http://A_PONE_APPS_HOST:8080/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 120s;
        proxy_connect_timeout 10s;
    }

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}